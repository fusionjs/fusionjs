# NOTE: projects and flags are dynamically generated from manifest.json
# in .buildkite/generate-codecov.sh

parsers:
  javascript:
    enable_partials: 'yes'
# docs on codecov flags: https://docs.codecov.io/docs/flags
coverage:
  status:
    project:
      default: off
      # [package name]:
      #   flags: [package name]
flags:
  # [package name]:
  #   paths:
  #     - [package path]
