// @flow

/*::
export type Schema = {
  version: string,
  server: Array<DepData>,
  browser: Array<DepData>,
  runtime: Metadata,
}

export type DepData = {
  timestamp: number,
  dependencies: Array<Dep>,
};

export type Dep = {
  name: string,
  type: 'value' | 'service' | 'middleware' | 'both' | 'noop',
  sources: Array<Source>,
  dependencies: Array<string>,
};

export type Source = {
  type: 'token' | 'plugin' | 'register' | 'enhance' | 'alias-from' | 'alias-to',
  source: string,
};

export type Metadata = {
  timestamp: number,
  pid: number,
  nodeVersion: string,
  npmVersion: string,
  yarnVersion: string,
  lockFileType: string,
  lockFile: string,
  dependencies: {[string]: string},
  devDependencies: {[string]: string},
  varNames: [string],
  vars: {[string]: string},
}
*/
