{
  "name": "a",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "mkdir -p ../node_modules/.bin && touch ../node_modules/.bin/flow && chmod +x ../node_modules/.bin/flow",
    "test": "echo a test",
    "lint": "echo a lint",
    "flow": "echo a flow"
  }
}