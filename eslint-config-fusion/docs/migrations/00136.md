#### Install new peer dependency `eslint-plugin-react-hooks`

Install `eslint-plugin-react-hooks` as dev dependency:

```
yarn add eslint-plugin-react-hooks --dev
```

or using npm:

```
npm i eslint-plugin-react-hooks --save-dev
```
